[phases.setup]
nixPkgs = ["php82", "nodejs_20"]

[phases.install]
cmds = [
  "npm install",
  "composer install --no-dev --optimize-autoloader"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "php-fpm"
